export type ProgressBarProps = {
  trackingElementId: string;
};

export type ProgressBarFactoryStyles = {
  Container?: string;
  ProgressBar: string;
};

export type ProgressBarFactoryOptions = {
  styles: ProgressBarFactoryStyles;
};
