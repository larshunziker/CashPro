import { ReactElement } from 'react';

export type HeaderAdZoneFactoryOptions = {
  /* @ts-ignore TODO: TS7006 ->  Parameter 'props' implicitly has an 'any' type. */
  MonsterSky: (props) => ReactElement;
  /* @ts-ignore TODO: TS7006 ->  Parameter 'props' implicitly has an 'any' type. */
  LeaderBoard: (props) => ReactElement;
};
