# ras-frontend nginx

nginx that sits in front of ras-frontend nodejs servers for redirects and other things that can be easier done in nginx instead of node.

## Configure

See `proxy.template` and adapt

## Test

Run `docker-compose up` within the `.nginx` folder
