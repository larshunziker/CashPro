import type { ArticleLeadProps } from '../../typings';

export type ArticleLeadRatgeberProps = ArticleLeadProps;

export type ArticleLeadShortTitleProps = {
  shortTitle: string;
  isAdvertorial: boolean;
  restrictionStatus: string;
};
