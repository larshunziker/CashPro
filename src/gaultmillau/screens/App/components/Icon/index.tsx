/* istanbul ignore file */

import iconFactory from '../../../../../common/components/Icon/factory';
import iconFont from '../../assets/styles/gaultMillau.legacy.css';

const Icon = iconFactory({
  iconFont,
});

export default Icon;
