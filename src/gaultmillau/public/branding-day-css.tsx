'strict';
/* eslint-disable no-console */
(function () {
  function getBrandingDayCss(
    {
      // imageUrls = [],
      // bgColor = '',
      // bgFixed = true,
      // paddingTop = 0,
      // bgCover = false,
      // hideHpa = true,
      // debug = false,
      // isImageUrlPresent = false,
    },
  ) {
    const brandingDayCss = '';

    const smallImageCss = '';

    const largeImageCss = '';

    const expandTopClickWrapper = null;

    return {
      brandingDayCss,
      largeImageCss,
      smallImageCss,
      expandTopClickWrapper,
    };
  }
  /* @ts-ignore TODO: TS2322 ->  Type '({} */
  window.getBrandingDayCss = getBrandingDayCss;
})();
